{
  "name": "new-ids-canoe-compensation",
  "version": "1.0.0",
  "description": "Data pipeline for new CANOE compensation IDs.",
  "scripts": {
    "test": "echo 'Running dummy tests...' && exit 0"
  },
  "devDependencies": {
    "semantic-release": "^23.0.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/changelog": "^6.0.3"
  }
}